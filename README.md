Cách chạy chương trình
Backend
1. để file env vào folder BE
2. npm install
3. npm run dev
Lưu ý: nếu cần ACCESS_TOKEN_SECRET thì gõ: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
============
Frontend 
1. npm install
2. npm run dev
============
Mobile
1. npm install 
2. npx expo start
